WeatherTower
	subscribed
	weatherMap
	updateWeather()

weather (enum ?)
	RAIN
	FOG
	SUN
	SNOW

Aircraft
	JetPlane
		rain : longitude 0  ; latitude 5  ; height 0
		fog  : longitude 0  ; latitude 1  ; height 0
		sun  : longitude 0  ; latitude 10 ; height 2
		snow : longitude 0  ; latitude 0  ; height -7
	Helicopter
		rain : longitude 5  ; latitude 0  ; height
		fog  : longitude 1  ; latitude 0  ; height
		sun  : longitude 10 ; latitude 0  ; height 2
		snow : longitude    ; latitude    ; height -12
	Baloon
		rain : longitude    ; latitude 0  ; height -5
		fog  : longitude    ; latitude 0  ; height -3
		sun  : longitude 2  ; latitude 0  ; height 4
		snow : longitude    ; latitude 0  ; height -15

	id (unique)
	longitude
	latitude
	height
	weatherTower

	register(weatherTower)
	unregister();

	log()
	land()

MAX_HEIGHT 100
MIN_HEIGHT 0

if (height + heightModifier <= 0)
	aircraft.land()